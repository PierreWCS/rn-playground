import React from 'react';
import {Text} from 'react-native';
import {ScreenWrapper} from '../../../theme';

const Dashboard = () => {
  return (
    <ScreenWrapper>
      <Text style={{textAlign: 'center', fontSize: 24}}>Dashboard</Text>
      <></>
    </ScreenWrapper>
  );
};

export default Dashboard;
